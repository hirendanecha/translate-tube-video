export interface BreakpointFlags {
  xs: {
    lessThen: boolean,
    between: boolean,
    gatherThen: boolean,
  },
  sm: {
    lessThen: boolean,
    between: boolean,
    gatherThen: boolean,
  },
  md: {
    lessThen: boolean,
    between: boolean,
    gatherThen: boolean,
  },
  lg: {
    lessThen: boolean,
    between: boolean,
    gatherThen: boolean,
  },
  xl: {
    lessThen: boolean,
    between: boolean,
    gatherThen: boolean,
  },
  xxl: {
    lessThen: boolean,
    between: boolean,
    gatherThen: boolean,
  },
}
